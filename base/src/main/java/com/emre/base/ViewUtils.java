package com.emre.base;

import android.view.View;

public class ViewUtils implements ViewInterface {


    @Override
    public void changeViewVisibility(View parent, View viewToChange) {
        if (viewToChange.getVisibility()==View.VISIBLE){

        }
    }
}
